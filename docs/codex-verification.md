# Codex 学习接入验收（2026-09-23）

## v1.10.0 自动练习更新

- 219 项应用、API 与 PostgreSQL 测试、29 项 CLI 测试通过，类型检查与生产构建通过。本次没有改 Keychain 存储逻辑；原生 Keychain 测试默认跳过，实际存取已在 v1.9.0 验证。
- 新材料接口沿用原 SRS 的初始化、日期与排期；验证更早到期优先、相同到期日从完整候选随机抽取、旧语言/词数偏好不限制默认批次，以及读取不会重置已有状态。
- 验证混合词句、纯句和空库、材料不足时补足可用来源、混合会话与纯句总结；句子不会生成虚构单词或评分。
- 新词验证包括独立权限、同 ID 重试、重复词不覆盖、日期缺省跨时区重试、跨账号隔离、删除后的旧请求不会复活词条。
- 本机 API 对真实云数据库通过 103 项隔离账号检查，包括并发添加同一词仅产生一条、旧连接经网页登录原地增加权限、英德语自动取材与共用复习进度。生产部署使用同一 `verify:api` 脚本验收。
- 浏览器检查精简设置页的桌面、390px 手机、深浅色样式，无横向溢出或运行错误；实际点击旧连接的「允许保存新词」并确认服务端更新成功。
- 独立复核发现全局词条去重触发器会干扰旧网页离线同步，因此最终迁移不包含该触发器；去重和稳定请求收据仅用于新词 API，网页原写入行为不变。
- Skill 场景复核：普通开始无需选择语言/数量；纯句子可复用素材生成题目但不伪造 SRS；用户不保存后单独请求添加仅授权该项，已知缺权限不提交或排队。

## 已验证

- 类型检查、应用测试、数据库测试、CLI 测试和 Vercel 生产构建。
- PGlite 执行真实增量 SQL：权限、账号归属、事件幂等、会话版本、SRS 与前端预览一致、删除后的历史保留、夏令时及迟到事件。
- 独立子任务复核：只读语音练习不写数据；听不清不评分；恢复未知回执使用原 UUID；用户说“不保存”后停止保存和重试。
- 本机 macOS Keychain 的实际新增、读取、更新、删除，使用隔离假令牌并清理。
- 已部署的正式域名通过54项隔离账号检查，覆盖词汇查询、初始化排期、会话创建/读取/完成、作答保存、同ID重试/冲突、并发重试只执行一次、收藏句、权限与撤销。
- 浏览器实际操作：连接令牌创建与一次性显示、偏好保存、练习历史/作答反馈；桌面与390px手机布局。
- 实际浏览器离线作答与同时发生的云端作答：离线事件保留、恢复联网后队列清空，较新的云端排期未被覆盖。
- 生产保护迁移后，旧客户端直接修改复习状态得到403；新事件API仍能正常写入。
- 本机已安装Skill，并通过系统钥匙串中的用户令牌，实际读取正式生词本与复习批次。

## 部署检查发现并修复

- 普通 Vercel Function 不按 Next.js 的 catch-all 方式处理多级路径；使用显式 rewrite，将版本化 API 映射到统一函数入口。
- 本地 Bun 能解析无扩展名导入，部署的 Node ESM 需要 `.js` 扩展名；修正后已验证编译产物可加载及生产调用成功。
- 固定 Vercel 的 Bun 安装/构建命令和 packageManager，避免平台生成的其他锁文件改变依赖解析。

## 仍需真人体验

Codex 实时语音的真实听说、打断、发音听辨及延迟，需要用户开启麦克风参与一轮练习。本次未将文字或HTTP测试当作语音验收。

## 平台检查备注

学习表和新增词汇写入收据表启用RLS且不给客户端任何表权限，只有已鉴权的服务端能访问；因此数据库检查器显示的“RLS enabled, no policy”是预期的关闭访问状态。平台另有既存的[泄漏密码保护未开启提示](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)，此次没有改变账号的全局密码策略。

测试账号、词汇和待测凭据均与真实学习数据隔离；使用 `scripts/cleanup-learning-fixture.ts` 清理保留的浏览器夹具。
